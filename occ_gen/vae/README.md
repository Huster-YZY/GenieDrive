# VAE Training

This directory provides scripts for training and evaluating the VAE model.

## Train VAE

Run the following command to train the VAE:

```bash
bash train.sh
```

## Evaluate VAE

Run the following command to evaluate the VAE:

```bash
bash eval.sh
```